import type { Metadata } from "next";
import * as React from "react"

export const metadata: Metadata = {
  title: "Cranberry - Productos",
  description: "Hacemos envios a todo el pais.",
};

export default function ProductsPage() {
  return (
    <React.Fragment>
      Productos
    </React.Fragment>
  );
}
